/**
 * Pure utility functions.
 * No side effects, no framework dependencies.
 */

// Placeholder – add helpers, e.g.:
// export const formatDate = (d: Date) => d.toISOString().slice(0, 10)

export {};
